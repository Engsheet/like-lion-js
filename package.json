{
  "scripts": {
    "start": "live-server client --host=localhost --port=5500 --mount=/node_modules:./node_modules",
    "serve": "node server/index.js"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "live-server": "^1.2.2",
    "prettier": "^2.8.8"
  }
}
